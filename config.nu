# Installed by:
# version = "0.105.1"

source ./starship/init.nu; # load the starship configuration
source ./televisor/config.nu; # load the televisor config

# Plugins
source ~/.cache/carapace/init.nu;
source ~/.zoxide.nu;

use ./utils *;
